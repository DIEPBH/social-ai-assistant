from django.apps import AppConfig


class SocialMessagesConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'social_messages'
    verbose_name = 'Quản lý tương tác'
